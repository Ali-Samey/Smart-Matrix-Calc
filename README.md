### Key Features
- **Full Arithmetic Support:** Handles positive, negative, and floating-point numbers (using `double`).
- **Dynamic Allocation:** Handles matrices of any size (N x M) using `double**` logic.
- **Operations:** Supports Matrix Addition and Multiplication.
- **Memory Safety:** Explicitly manages memory using `new` and `delete[]` to prevent leaks.
